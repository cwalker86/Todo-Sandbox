import {
  AppRegistry,
} from 'react-native';
import App from "./app";

// App is the default component that will be registered with the native world.
AppRegistry.registerComponent('todo', () => App);
